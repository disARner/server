const { describe } = require('mocha')
const chai = require('chai')
const { expect } = chai
const { sequelize, Cart, User } = require('../models')
const { queryInterface } = sequelize

describe('CartItem DB TEST', () => {
  
})
